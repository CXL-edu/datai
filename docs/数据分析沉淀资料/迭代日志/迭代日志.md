# 迭代日志

缺失值填充方式应该由模型选择，并给出原因，而不是直接使用当前列的均值进行填充data\_cleaned\[numeric\_cols].fillna(data\_cleaned\[numeric\_cols].mean())

\`生成统计摘要\`应该在\`提取分析诉求\`之前，而且缺失值处理应该放在统计摘要中的后面部分进行

data\_workflow(st.session\_state.data\_theme, st.session\_state.data)

[2025年](2025年/2025年.md "2025年")
